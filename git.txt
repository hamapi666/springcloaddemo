git查看邮箱，用户名
git config user.name/email

git config --global user.name "zhangsan(新的用户名)"
git config --global user.email "123456@qq.com(新的邮箱)"
git config --global user.password "123456(新的密码)"

————————————————
…or create a new repository on the command line

echo "# springcloaddemo" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/hamapi666/springcloaddemo.git
git push -u origin master
                

…or push an existing repository from the command line

git remote add origin https://github.com/hamapi666/springcloaddemo.git
git push -u origin master